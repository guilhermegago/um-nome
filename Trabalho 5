1 – implemente o algoritmo de ordenação Bubble Sort usando python;
def bubble_sort(arr):
    n = len(arr)

    for i in range(n):
        # Últimos i elementos já estão ordenados, então não precisamos checar
        for j in range(0, n-i-1):
            # Trocar se o elemento encontrado for maior que o próximo
            if arr[j] > arr[j+1]:
                arr[j], arr[j+1] = arr[j+1], arr[j]

# Exemplo de uso
arr = [64, 34, 25, 12, 22, 11, 90]
bubble_sort(arr)
print("Array ordenado:", arr)
